import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    wrapper: {
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
        gap: 20
    },
    paper: {
        padding: 20
    }
});
